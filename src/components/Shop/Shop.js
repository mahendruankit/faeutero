import React, { forwardRef } from 'react';

import './Shop.scss';

const Shop = forwardRef((props, ref) => {
  return (
    <section className='shop' ref={ref}>
      COMING SOON
    </section>
  );
});

export default Shop;
